export const meta = {
  id: 1,
  title: 'Hello from MDX'
}

## This is a subtitle

```tsx
import { FC } from 'react';

type CardProps = {
  title: string,
  paragraph: string
}

export const Card: FC<CardProps> = ({ title, paragraph }) => <aside>
  <h2>{ title }</h2>
  <p>
    { paragraph }
  </p>
</aside>
```